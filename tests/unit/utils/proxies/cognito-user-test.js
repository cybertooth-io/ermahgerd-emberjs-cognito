import CognitoUserProxy from 'ermahgerd-emberjs-cognito/utils/proxies/cognito-user';
import { module, test } from 'qunit';

module('Unit | Utility | proxies/cognito-user', function (/*hooks*/) {

  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = new CognitoUserProxy();
    assert.ok(result);
  });
});
